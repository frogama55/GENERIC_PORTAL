# CITポータル改造 Chrome拡張 — 実装・検討 指示書

> このドキュメントはClaude Codeに渡す前提の指示書です。上から順に進めてください。各フェーズの完了時に必ず立ち止まり、私（依頼者）に確認を取ってから次へ進むこと。勝手に先のフェーズへ進まない。

---

## 0. プロジェクトの目的

既存のCITポータル（大学の学生／教職員向けポータル。千葉工業大学を想定）が使いづらいため、**自分専用の使いやすいポータルUIを、Chrome拡張機能として自作する**。

重要な設計思想：

- データは**自分のブラウザ内で完結**させる。外部サーバーに送信・保存しない。
- サーバーへの追加負荷を最小限にする（人間の操作と同程度のリクエスト頻度に抑える）。
- **公開しない**。自分専用。Chromeウェブストアには出さない前提で作る。
- 規約・倫理上グレーな機能（自動再ログイン等）は実装しない。代替の安全な方法を採る。

---

## 1. 最初にやること（コードを書く前）

### 1-1. 規約・環境の確認（依頼者へのヒアリング）

コードを書き始める前に、以下を私に質問して確認すること。答えが揃うまで実装に入らない。

1. 対象ポータルの正確なURL（複数ドメインにまたがるか？ SSO/IdPは別ドメインか？）
2. ログイン方式（ID/パスワードのみ / 多要素認証(MFA)の有無 / SSOか）
3. 大学の「情報システム利用規程」または「ポータル利用規約」に、次の条項があるか：
   - 自動化されたアクセス・スクレイピングの可否
   - 認証情報を第三者ソフトウェアに入力・保持することの可否
4. 普段どの情報をよく見るか（時間割 / 休講情報 / 課題 / 成績 / お知らせ / etc）＝改造UIの優先表示項目
5. 使うブラウザはChromeのみか（Edge/Firefox対応も要るか）

### 1-2. 対象ページの構造調査

- ログイン後の主要ページについて、DOM構造・主要データがどのHTML要素に入っているかを調査するための手順を提案する（開発者ツールでの確認手順を私に指示する形でよい。私のログイン情報を使う操作はすべて私自身が手元で行う）。
- スクレイピング対象の要素セレクタを洗い出し、`docs/selectors.md` にまとめる。

---

## 2. 技術方針

- **Manifest V3** で実装する。
- 構成要素：
  - `content script`：対象ページのDOM/CSSを改変
  - `background (service worker)`：定期処理・状態管理
  - `chrome.storage`：キャッシュ・下書き退避（外部送信は一切しない）
  - 自作ポータル本体は **拡張のサイドパネル または 新規タブページ(new tab override は任意)** として実装。まずはサイドパネルを推奨。
- パースは対象ページの同一オリジンへの `fetch`（ログイン済みcookieが効く）で取得したHTMLをDOMParserで処理する。
- 依存は最小限に。ビルドが必要なら Vite + TypeScript を提案（ただし過剰にしない）。

---

## 3. 実装フェーズ（段階的に。各フェーズ末で停止して確認）

### フェーズ1：見た目だけ改変（規約リスク最小）

まずここだけで使い勝手を大きく改善する。データ取得・通信は一切しない。

- content scriptでCSS注入・DOM整形（不要要素を隠す、レイアウト再構成、フォント・余白調整、ダークモード等）
- 設定で改変のON/OFFを切り替えられるようにする
- **成果物**：この状態で一度私が実際に使って評価する。ここで止まること。

### フェーズ2：情報の集約表示（読み取りのみ）

- ログイン済み状態を前提に、主要ページのHTMLを取得→パース→自作ポータル(サイドパネル)に集約表示
- リクエストは**私が明示的に開いた／更新したときのみ**発火。バックグラウンドの自動ポーリングはこの段階では入れない
- 取得データは `chrome.storage` にキャッシュし、オフライン／再訪時に前回内容を即表示
- **外部送信は絶対にしない**ことをコード・レビューで担保する
- **成果物**：集約ビュー。ここで止まって評価。

### フェーズ3：セッション切れ対策（安全な方法のみ）

以下の**やらないこと**を厳守する：

- ❌ パスワードを拡張内に保持しない
- ❌ 自動再ログインを実装しない
- ❌ セッション延命のための定期ping（サーバー負荷・セキュリティ意図の無効化になるため）は原則入れない

代わりに実装すること：

- ✅ セッション切れを**検知して通知**（ログイン画面へのリダイレクト等を検出）
- ✅ 切れていたらワンクリックでログインページを開くボタンを出す
- ✅ フォーム入力途中のデータを `chrome.storage` に退避し、再ログイン後に復帰させる
- （もし 3-a. の定期取得を将来入れる場合でも、頻度は人間の操作と同程度＝分単位で十分間隔を空け、設定でOFFをデフォルトにする。導入是非は必ず私に相談する）

### フェーズ4：仕上げ

- 設定画面、エラーハンドリング、README（自分用のインストール・使い方手順）
- `chrome://extensions` へのデベロッパーモード読み込み手順を明記

---

## 4. 全フェーズ共通の制約（ハードルール）

1. 取得したいかなるデータも**外部（拡張外のサーバー・API）へ送信しない**。テレメトリ・分析ツールも入れない。
2. 認証情報（ID/パスワード/トークン）を保存・自動入力しない。
3. リクエスト頻度は人間の操作と同程度に抑える。ループでの連続取得や短間隔ポーリングを入れない。
4. 対象サイトの規約に抵触しうる実装を思いついたら、実装せずに私に相談する。
5. コードはすべてローカル。npm等の依存追加時は理由を添えて事前に知らせる。
6. 破壊的操作（ポータル側へのPOST/データ変更）は行わない。読み取り専用に徹する。

---

## 5. 最初のアウトプット

まず **セクション1-1のヒアリング質問**を私に投げること。その回答を待ってから、フェーズ1の実装計画（ファイル構成・manifest.jsonのドラフト）を提示する。コードの本格実装はそのあと。

---

## 付録：想定ディレクトリ構成（暫定・変更可）

```
cit-portal-ext/
  manifest.json
  src/
    content/        # DOM/CSS改変
    panel/          # 自作ポータルUI（サイドパネル）
    background/     # service worker
    lib/            # パーサ・ストレージ・共通処理
  docs/
    selectors.md    # 対象ページの要素調査結果
    privacy.md      # データを外部送信しないことの設計メモ
  README.md
```
